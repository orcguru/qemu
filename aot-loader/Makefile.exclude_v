ARCH_HOST := $(shell uname -m)
CFLAGS = -O2 -g -Wall
ifeq ($(ARCH_HOST), riscv64)
    CFLAGS += -march=rv64g
endif

all: libaotloader.a

libaotloader.a: light_jitlink.o qemu_lightjit.o
	ar rcs $@ $^

%.o: %.cpp
	g++ $(CFLAGS) -c $< -o $@
clean:
	rm -f *.o libaotloader.a
